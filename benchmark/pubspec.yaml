name: dartic_benchmark
description: Performance benchmark suite for dartic interpreter.
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  dartic:
    path: ..
  dart_eval: ^0.8.4

dev_dependencies:
  test: ^1.25.6
  lints: ^6.0.0
